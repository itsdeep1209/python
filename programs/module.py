#Module in a python is a file containing python functions , statements and definitions just like a code library , there are two type of modules 1) system module 2) custome module
# system module : there are several built in python that came default with python , we can use the system module by using import statement.
#custom module : we can use modules to break down large programs into a small manageable and organized files 

import sys
print(sys.version)